//제어 흐름
#include <iostream>

using namespace std;

int main()
{
	//배열에서 첫번째 음수값이 나타날 때까지 원소의 합을 계산
	/*int a[100]; //->초기화하는 구문이 필요

	int i = 0;
	int sum = 0;
	while (i < 100 && a[i] >= 0) {
		sum += a[i++];
	}
	cout << sum;*/
}